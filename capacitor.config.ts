import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.thedifference.adminclub',
  appName: 'AdminClub',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
